*** Comments ***
# ==============================================================================
# 檔案用途：udemy_locators.robot
# ==============================================================================
# 這個檔案「只」負責一件事：把所有 Selector（元素定位器）集中放在這裡管理。
#
# 為什麼要這樣做？
# - 如果 Udemy 網頁改版，某個按鈕的 XPath 變了，你只需要來這個檔案改一次，
#   所有引用到這個變數的測試案例會「自動同步更新」，不用一個一個檔案去找。
# - 測試案例本身不會直接出現又長又難懂的 XPath 字串，可讀性大幅提升。
#
# 這個檔案「不會」被直接執行（沒有 *** Test Cases *** 區塊），
# 它是被其他 .robot 檔案用 Resource 關鍵字「引用」進去使用的。
# ==============================================================================


*** Variables ***
# ------------------------------------------------------------------------
# 命名規則建議：全部大寫 + 底線，一看就知道是「固定的 Selector 常數」，
# 跟一般測試資料變數（例如 ${username}）做出視覺區隔。
# ------------------------------------------------------------------------

# 首頁搜尋輸入框
# 【重要發現】GitHub Actions 雲端伺服器的 IP 位於海外資料中心，
# Udemy 會根據造訪者 IP 判斷地區並自動切換語言，導致雲端環境看到的是「英文版」頁面
# （placeholder 顯示 "Search for anything"），而不是你本機台灣網路看到的「中文版」
# （placeholder 顯示 "搜尋任何事物"）。這正是雲端排程一直失敗、本機卻正常的根本原因。
#
# 用 or 條件同時比對中英文兩種版本的 placeholder 文字，不管哪個地區、哪個語言版本都能抓到。
${SEARCH_BOX}          xpath://input[@placeholder='搜尋任何事物' or @placeholder='Search for anything']

# 導覽列「探索」連結
# contains(.,'探索') 的意思：這個元素「本身或裡面任何子層級」的文字只要包含「探索」就算符合
# 用 . 而不是 text()，是因為之前實測發現文字可能被包在裡面的 <span> 標籤，
# text() 只抓「直接」子層文字，抓不到孫層，會導致誤判找不到元素。
${NAV_EXPLORE}         xpath://a[contains(.,'探索')]

# 導覽列「登入」連結，邏輯同上
${NAV_LOGIN}           xpath://a[contains(.,'登入')]

# 課程頁面的標題（h1 標籤，通常整頁只會有一個主標題）
${COURSE_TITLE}        xpath://h1

# 課程頁面的價格資訊（寬鬆版本）
# 改用最寬鬆的方式：只要頁面上「任何地方」有出現美金符號 $，就算通過。
#
# 【為什麼又改了一次】
# 追蹤特定 class 名稱（class="_title-price_xxx"）失敗，
# 追蹤特定按鈕文字（新增至購物車／加入購物車）也失敗。
# 這代表右側購買面板本身載入時機、呈現版本都不穩定，很可能不同次造訪、
# 不同地區、有沒有登入，畫面結構都不一樣，沒辦法用「精確定位單一元素」的方式穩定驗證。
#
# 改成用「文字內容特徵」去找：不管價格顯示在哪個標籤、哪個 class 裡，
# 只要畫面上「有出現 $ 這個符號」，就代表價格資訊有正常顯示出來，
# 這是目前能想到最不容易被前端結構變動影響的驗證方式。
#
# xpath 語法解釋：// * 代表「不限定標籤種類，隨便什麼元素都算」，
# contains(text(),'$') 則是檢查這個元素的文字裡有沒有包含 $ 符號。
${PRICE_INFO}    xpath://*[contains(text(),'$')]
